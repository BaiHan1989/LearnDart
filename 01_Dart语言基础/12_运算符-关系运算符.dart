main(List<String> args) {
  // 关系运算符
  // 相等运算符
  print((3 + 2) == (2 + 3));

  // 不等运算符
  print((1 + 4) != (6 - 1));

  // 大于运算符
  print((1 + 4) > (6 - 1));

  // 大于等于运算符
  print((1 + 4) >= (6 - 1));

  // 小于运算符
  print((1 + 4) < (6 - 1));

  // 小于等于运算符
  print((1 + 4) <= (6 - 1));
}
